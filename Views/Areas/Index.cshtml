@model IEnumerable<Carbon_inventory_platform.Models.Area>

<div class="row">
    <div class="col">
        <h1>邊界總覽</h1>
        <h3>@TempData.Peek("companyName")</h3>
        <a class="btn btn-primary" asp-action="Create" asp-route-id=@TempData.Peek("selectedCompanyId")>新增</a>
        <a class="btn btn-secondary" asp-controller="Home" asp-action="Index">返回</a>
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            @* <th>
                @Html.DisplayNameFor(model => model.PostalCode)
            </th> *@
            <th>
                @Html.DisplayNameFor(model => model.FullAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Year)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th></th>
            <th></th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
               @*  <td>
                    @Html.DisplayFor(modelItem => item.PostalCode)
                </td> *@
                <td>
                    @Html.DisplayFor(modelItem => item.FullAddress)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Year)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Type)
                </td>
                <td>
                    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id">修改</a>
                    <a class="btn btn-info" asp-controller="Years" asp-action="Index" asp-route-id="@item.Id">盤查年資料</a>
                </td>
                <td>
                    <form method="post" asp-action="Delete" asp-route-id="@item.Id" onsubmit="return confirm('確定要刪除嗎？');">
                        <button type="submit" class="btn btn-danger">刪除</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
